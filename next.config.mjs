/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1', 'localhost' , 'herbalapi.trygon.tech' , 'dev.api.quixgo.com'],
        // Add your production domain when deploying
      },
};

export default nextConfig;
